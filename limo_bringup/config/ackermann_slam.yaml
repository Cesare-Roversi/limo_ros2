
#! behavior trees preinstallati qui:
#! /opt/ros/humble/share/nav2_bt_navigator/behavior_trees/

# bt_navigator
# legge da /odom
bt_navigator:
  ros__parameters:
    use_sim_time: True
    global_frame: map
    robot_base_frame: base_link
    odom_topic: /odom
    default_nav_to_pose_bt_xml: "$(find-pkg-share limo_bringup)/config/behaviour_trees/ackermann_navigate_to_pose_w_replanning_and_recovery.xml"
    default_nav_through_poses_bt_xml: "$(find-pkg-share limo_bringup)/config/behaviour_trees/ackermann_navigate_through_poses_w_replanning_and_recovery.xml"



# https://www.yahboom.net/public/upload/upload-html/1669599741/14.2%20Kinematics%20Analysis%20of%20Ackermann%20Car.html
#* https://github.com/ros-navigation/navigation2/tree/humble/nav2_smac_planner
planner_server:
  ros__parameters:
    planner_plugins: ["GridBased"]
    use_sim_time: True

    GridBased:
      plugin: "nav2_smac_planner/SmacPlannerHybrid"
      #! PRACTICAL:
      tolerance: 0.5                      # tolerance for planning if unable to reach exact pose, in meters
      allow_unknown: true                # allow traveling in unknown space
      motion_model_for_search: "REEDS_SHEPP"    # For Hybrid Dubin, Redds-Shepp
      #! I USE HIBRID NOT 2D!! cost_travel_multiplier * global_costmap_value
      # cost_travel_multiplier: 2.0         # For 2D: Cost multiplier to apply to search to steer away from high cost areas. Larger values will place in the center of aisles more exactly (if non-`FREE` cost potential field exists) but take slightly longer to compute. To optimize for speed, a value of 1.0 is reasonable. A reasonable tradeoff value is 2.0. A value of 0.0 effective disables steering away from obstacles and acts like a naive binary search A*.
      #* tende a cercare lontano dalle zone costose della global costmap
      cost_penalty: 3.5                   # For Hybrid nodes: penalty to apply to higher cost areas when adding into the obstacle map dynamic programming distance expansion heuristic. This drives the robot more towards the center of passages. A value between 1.3 - 3.5 is reasonable.
      minimum_turning_radius: 0.40        # For Hybrid/Lattice nodes: minimum turning radius in m of path / vehicle
      reverse_penalty: 1.0 #! era 2.1               # For Reeds-Shepp model: penalty to apply if motion is reversing, must be => 1
      change_penalty: 0.8 #! era 0.5                 # For Hybrid nodes: penalty to apply if motion is changing directions, must be >= 0
      non_straight_penalty: 1.2 #! era 1.2          # For Hybrid nodes: penalty to apply if motion is non-straight, must be => 1
      retrospective_penalty: 0.025        # For Hybrid/Lattice nodes: penalty to prefer later maneuvers before earlier along the path. Saves search time since earlier nodes are not expanded until it is necessary. Must be >= 0.0 and <= 1.0
      # rotation_penalty: 5.0               # For Lattice node: Penalty to apply only to pure rotate in place commands when using minimum control sets containing rotate in place primitives. This should always be set sufficiently high to weight against this action unless strictly necessary for obstacle avoidance or there may be frequent discontinuities in the plan where it requests the robot to rotate in place to short-cut an otherwise smooth path for marginal path distance savings.
      #* smoother:
      smooth_path: True                   # For Lattice/Hybrid nodes: Whether or not to smooth the path, always true for 2D nodes.
      smoother:
        max_iterations: 1000
        w_smooth: 0.3
        w_data: 0.2
        tolerance: 1.0e-10
        do_refinement: true               # Whether to recursively run the smoother 3 times on the results from prior runs to refine the results further

      #! JUST FOR ALGORITHM:
      downsample_costmap: false           # whether or not to downsample the map
      downsampling_factor: 1              # multiplier for the resolution of the costmap layer (e.g. 2 on a 5cm costmap would be 10cm)
      #* max_iterations = infinite
      max_iterations: -1             # maximum total iterations to search for before failing (in case unreachable), set to -1 to disable
      max_on_approach_iterations: 1000    # maximum number of iterations to attempt to reach goal once in tolerance
      #* era 3.5 s
      max_planning_time: 40.0              # max time in s for planner to plan, smooth, and upsample. Will scale maximum smoothing and upsampling times based on remaining time after planning.
      angle_quantization_bins: 64         # For Hybrid nodes: Number of angle bins for search, must be 1 for 2D node (no angle search)
      analytic_expansion_ratio: 3.5       # For Hybrid/Lattice nodes: The ratio to attempt analytic expansions during search for final approach.
      analytic_expansion_max_length: 3.0    # For Hybrid/Lattice nodes: The maximum length of the analytic expansion to be considered valid to prevent unsafe shortcutting (in meters). This should be scaled with minimum turning radius and be no less than 4-5x the minimum radius
      lookup_table_size: 20.0               # For Hybrid nodes: Size of the dubin/reeds-sheep distance window to cache, in meters.
      cache_obstacle_heuristic: True      # For Hybrid nodes: Cache the obstacle map dynamic programming distance expansion heuristic between subsiquent replannings of the same goal location. Dramatically speeds up replanning performance (40x) if costmap is largely static.  
      # allow_reverse_expansion: False      # For Lattice nodes: Whether to expand state lattice graph in forward primitives or reverse as well, will double the branching factor at each step.   
      
#! non trova la path da un angolo all'altro della mappa!!!!
#! max plainning time di 40s è ridicolo!!!


# https://docs.ros.org/en/humble/p/nav2_regulated_pure_pursuit_controller/
controller_server:
  ros__parameters:
    use_sim_time: True
    controller_frequency: 20.0
    min_x_velocity_threshold: 0.001
    min_y_velocity_threshold: 0.5
    min_theta_velocity_threshold: 0.001
    odom_topic: /odom
    failure_tolerance: 0.5
    progress_checker_plugin: "progress_checker"
    goal_checker_plugins: ["goal_checker"]
    controller_plugins: ["FollowPath"]

    progress_checker:
      plugin: "nav2_controller::SimpleProgressChecker"
      required_movement_radius: 0.1 #10 cm in massimo
      movement_time_allowance: 60.0 #60 s
    goal_checker:
      plugin: "nav2_controller::SimpleGoalChecker"
      xy_goal_tolerance: 0.1
      yaw_goal_tolerance: 1.571  # +-90 gradi di tolleranza è tanto ma non fa impazzire il controller
      stateful: True
    FollowPath:
      plugin: "nav2_regulated_pure_pursuit_controller::RegulatedPurePursuitController"

      #* NAVIGAZIONE:
      desired_linear_vel: 0.6
      allow_reversing: true

      # collisioni
      use_collision_detection: true
      max_allowed_time_to_collision_up_to_carrot: 0.4 #* quanti secondi nel futuro guardare
      # è una curva in base a velocità lineare e angolare del robot, NON segue la path del planner quindi deve essere un valore basso

      # interpolazione x aggiungere punti alla path
      use_interpolation: true

      #* ROTAZIONE SUL POSTO:
      use_rotate_to_heading: false #* rotate_to_heading == girare sul posto, ackermann non può
      rotate_to_heading_angular_vel: 1.8
      rotate_to_heading_min_angle: 0.785
      max_angular_accel: 3.2 #*sempre riferito a rotazione sul posto
      

      #* SCALING NAVIGAZIONE:
      # rallentare alle curve:
      use_regulated_linear_velocity_scaling: true
      regulated_linear_scaling_min_radius: 0.8 #*raggio sotto il quale diminuisce la velocità
      regulated_linear_scaling_min_speed: 0.15 #*se use_regulated_linear_velocity_scaling: true

      # rallentare vicino a zone ad alto costo nella costmap (I muri tendenzialmente)
      use_cost_regulated_linear_velocity_scaling: true
      cost_scaling_dist: 0.4 # distanza minima dal quale l'algoritmo diminuisce velocità
      cost_scaling_gain: 0.5 # Velocità Scalata = Velocità Attuale * (cost_scaling_gain * (Distanza Reale / cost_scaling_dist))
      inflation_cost_scaling_factor: 3.0 #!DEVE ESSERE UGUALE A local_costmap / inflation_layer / cost_scaling_factor
      # il controller legge il costo dalla local costmap e vuole ricavare la distanza dall'ostacolo, quindi gli serve di sapere il fattore.

      # rallentare vicino al goal:
      min_approach_linear_velocity: 0.05
      approach_velocity_scaling_dist: 0.3

      #* ALGORITMO:
      # quanto vecchio può essere l'ultimo messaggio tf map -> odom (se il computer è lento alzalo)
      transform_tolerance: 0.5

      # distanza carrot = punto che il robot insegue
      lookahead_dist: 0.6

      # ricerca punto planner server path più vicino a base link
      max_robot_pose_search_dist: 10.0 # limite in m a questa ricerca

      #* SCALING ALGORITMO:
      # usa distaza carrot dinamica #!QUANTO TAGLIA LE CURVE?? CERCA!
      use_velocity_scaled_lookahead_dist: true 
      lookahead_time: 0.8 # quanto avanti è il carrot dinamico (minore = + vicino)
      min_lookahead_dist: 0.2 #* se use_velocity_scaled_lookahead_dist: true
      max_lookahead_dist: 0.8 #* se use_velocity_scaled_lookahead_dist: true


# il planner cerca la path a 1 metro (costmap globale)
# il controller rallenta bruscamente se si avvicina a 0.4 metri dal muro (calcolato tramite costmap LOCALE)


# behaviour server:
# decide cosa fare se il robot è bloccato;
# ! il robot ackermann non può usare spin??
# ! lo chiama quando è unable to do progress
behavior_server:
  ros__parameters:
    use_sim_time: true
    costmap_topic: local_costmap/costmap_raw
    footprint_topic: local_costmap/published_footprint
    cycle_frequency: 10.0
    behavior_plugins: ["backup", "drive_on_heading", "wait"]
    backup:
      plugin: "nav2_behaviors/BackUp"
    drive_on_heading:
      plugin: "nav2_behaviors/DriveOnHeading"
    wait:
      plugin: "nav2_behaviors/Wait"
    global_frame: odom
    robot_base_frame: base_link
    transform_tolerance: 0.1
    simulate_ahead_time: 2.0
    max_rotational_vel: 1.0
    min_rotational_vel: 0.4
    rotational_acc_lim: 3.2


waypoint_follower:
  ros__parameters:
    loop_rate: 20
    stop_on_failure: false
    waypoint_task_executor_plugin: "wait_at_waypoint"
    wait_at_waypoint:
      plugin: "nav2_waypoint_follower::WaitAtWaypoint"
      enabled: True
      waypoint_pause_duration: 200




local_costmap:
  local_costmap:
    ros__parameters:
      update_frequency: 5.0
      publish_frequency: 2.0
      #! prova, era: global_frame: map
      global_frame: odom
      robot_base_frame: base_link
      use_sim_time: True
      rolling_window: true #!sempre true
      #! pose goes off grid, aumento a 5 width e height
      width: 5
      height: 5
      resolution: 0.05
      # robot_radius: 0.22
      footprint: "[[0.15,0.12],[0.15,-0.12],[-0.15,-0.12],[-0.15,0.12]]"
      plugins: ["voxel_layer", "inflation_layer"]
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        cost_scaling_factor: 3.0 #! era 3.0
        # ERA inflation_radius: 0.1, MA NAV2 SI LAMENTAVA
        inflation_radius: 0.5 #!è poco proviamo
      voxel_layer:
        plugin: "nav2_costmap_2d::VoxelLayer"
        enabled: True
        publish_voxel_map: True
        origin_z: 0.0
        z_resolution: 0.05
        z_voxels: 16
        max_obstacle_height: 2.0
        mark_threshold: 0
        observation_sources: scan
        scan:
          topic: /scan
          max_obstacle_height: 2.0
          clearing: True
          marking: True
          data_type: "LaserScan"
          raytrace_max_range: 3.0
          raytrace_min_range: 0.0
          obstacle_max_range: 2.5
          obstacle_min_range: 0.0
      static_layer:
        map_subscribe_transient_local: True
      always_send_full_costmap: True



global_costmap:
  global_costmap:
    ros__parameters:
      rolling_window: true 
      update_frequency: 1.0
      publish_frequency: 1.0
      global_frame: map
      robot_base_frame: base_link
      use_sim_time: True
      footprint: "[[0.15,0.12],[0.15,-0.12],[-0.15,-0.12],[-0.15,0.12]]"
      resolution: 0.05
      track_unknown_space: true
      plugins: ["static_layer", "obstacle_layer", "inflation_layer"]
      obstacle_layer:
        plugin: "nav2_costmap_2d::ObstacleLayer"
        enabled: True
        observation_sources: scan
        scan:
          topic: /scan
          max_obstacle_height: 2.0
          clearing: True
          marking: True
          data_type: "LaserScan"
          raytrace_max_range: 3.0
          raytrace_min_range: 0.0
          obstacle_max_range: 2.5
          obstacle_min_range: 0.0
      static_layer:
        plugin: "nav2_costmap_2d::StaticLayer"
        map_subscribe_transient_local: True
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        cost_scaling_factor: 3.0
        # ERA inflation_radius: 0.1, MA NAV2 SI LAMENTAVA
        inflation_radius: 0.8 #! modificato
      always_send_full_costmap: True


map_server:
  ros__parameters:
    use_sim_time: True
    yaml_filename: ""

map_saver:
  ros__parameters:
    use_sim_time: True
    save_map_timeout: 5.0
    free_thresh_default: 0.25
    occupied_thresh_default: 0.65
    map_subscribe_transient_local: True

